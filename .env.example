# ---------- DEVELOPMENT (default) ----------
SECRET_KEY=django-insecure-CHANGE-THIS-GENERATE-A-REAL-ONE
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# ---------- PRODUCTION (uncomment & fill in on the server) ----------
# SECRET_KEY=<generate with: python -c "import secrets; print(secrets.token_urlsafe(50))">
# DEBUG=False
# ALLOWED_HOSTS=yourhospital.com,www.yourhospital.com
# DATABASE_URL=postgres://hospital_user:STRONG_PASSWORD@127.0.0.1:5432/hospital_db
